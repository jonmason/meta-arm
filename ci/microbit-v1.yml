header:
  version: 11
  includes:
    - ci/base.yml
    - ci/meta-zephyr.yml

local_conf_header:
  failing_tests: |
    ZEPHYRTESTS:remove = "common"

machine: microbit-v1

target:
  - zephyr-kernel-test-all
