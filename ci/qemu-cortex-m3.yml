header:
  version: 9
  includes:
    - base.yml
    - meta-zephyr.yml

local_conf_header:
  tclibc: |
    TCLIBC = "newlib"
  nonbuilding_tests: |
    ZEPHYRTESTS:remove = "common context pending poll sleep"

machine: qemu-cortex-m3

target:
  - zephyr-kernel-test-all
