header:
  version: 11
  includes:
    - ci/base.yml
    - ci/meta-zephyr.yml

local_conf_header:
  nonbuilding_tests: |
    ZEPHYRTESTS:remove = "common device poll queue sleep"

machine: qemu-cortex-a53

target:
  - zephyr-kernel-test-all
